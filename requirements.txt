# ConDiag v4 — Python Dependencies
# Install: pip install -r requirements.txt

# Core
minisweagent>=2.4.0
swebench>=4.1.0
datasets>=2.14.0
pyarrow>=12.0.0
docker>=6.0.0

# LLM access (via litellm, installed as minisweagent dependency)
# litellm>=1.0.0

# Utilities
tqdm>=4.65.0
